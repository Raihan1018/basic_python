# list is mutable, tuples is  immutable
fruits = ('apple', 'orange', 'mango', 'pineapple')

# fruits[1] = 'peach' # => tuples is immutable, we will get an error
# print(fruits)
# print(fruits[1])

print(fruits[1:4])